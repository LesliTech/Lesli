version: 2
jobs:
    build:
        working_directory: ~/my-app
        docker:
            - image: circleci/ruby:2.5.0-node-browsers
        steps:
            - checkout

            - run:
                name: Which bundler?
                command: bundle -v
