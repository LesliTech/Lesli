%h1 Listing lock_user_roles

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @lock_user_roles.each do |lock_user_role|
      %tr
        %td= link_to 'Show', lock_user_role
        %td= link_to 'Edit', edit_lock_user_role_path(lock_user_role)
        %td= link_to 'Destroy', lock_user_role, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User role', new_lock_user_role_path
