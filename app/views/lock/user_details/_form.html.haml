= form_for @lock_user_detail do |f|
  - if @lock_user_detail.errors.any?
    #error_explanation
      %h2= "#{pluralize(@lock_user_detail.errors.count, "error")} prohibited this lock_user_detail from being saved:"
      %ul
        - @lock_user_detail.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
