= form_for @lock_role_override do |f|
  - if @lock_role_override.errors.any?
    #error_explanation
      %h2= "#{pluralize(@lock_role_override.errors.count, "error")} prohibited this lock_role_override from being saved:"
      %ul
        - @lock_role_override.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
