= form_for @company_role_privilege do |f|
  - if @company_role_privilege.errors.any?
    #error_explanation
      %h2= "#{pluralize(@company_role_privilege.errors.count, "error")} prohibited this company_role_privilege from being saved:"
      %ul
        - @company_role_privilege.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
