= form_for @company_location_continent do |f|
  - if @company_location_continent.errors.any?
    #error_explanation
      %h2= "#{pluralize(@company_location_continent.errors.count, "error")} prohibited this company_location_continent from being saved:"
      %ul
        - @company_location_continent.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
