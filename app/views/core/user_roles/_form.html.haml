= form_for @core_user_role do |f|
  - if @core_user_role.errors.any?
    #error_explanation
      %h2= "#{pluralize(@core_user_role.errors.count, "error")} prohibited this core_user_role from being saved:"
      %ul
        - @core_user_role.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
