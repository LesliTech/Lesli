%h1 Listing core_user_details

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @core_user_details.each do |core_user_detail|
      %tr
        %td= link_to 'Show', core_user_detail
        %td= link_to 'Edit', edit_core_user_detail_path(core_user_detail)
        %td= link_to 'Destroy', core_user_detail, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User detail', new_core_user_detail_path
