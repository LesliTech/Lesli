<%= form_with(model: account_integration, local: true) do |form| %>
  <% if account_integration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account_integration.errors.count, "error") %> prohibited this account_integration from being saved:</h2>

      <ul>
        <% account_integration.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
