<%#

Copyright (c) 2022, all rights reserved.

All the information provided by this platform is protected by international laws related  to 
industrial property, intellectual property, copyright and relative international laws. 
All intellectual or industrial property rights of the code, texts, trade mark, design, 
pictures and any other information belongs to the owner of this platform.

Without the written permission of the owner, any replication, modification,
transmission, publication is strictly forbidden.

For more information read the license file including with this software.

// · ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~
// · 

%>

<%# Loading standar pdf viewer %>
<%# Temporary disable while moving to vue3 %>
<% if defined?(DeutscheLeibrenten) && false %>
    <application-component-panel-viewer-pdf></application-component-panel-viewer-pdf>
<% end %>


<%# Loading standard focus panel %>
<%# Temporary disable while moving to vue3 %>
<% if defined?(CloudFocus) && false %>
    <application-component-panel-task :instance-endpoint="'<%= lesli_instance_code == "deutsche_leibrenten" ? "crm" : "focus" %>'">
    </application-component-panel-task>
<% end %>


<%# Loading standar bell panels %>
<% if defined?(CloudBell) %>
    <application-component-panel-notifications></application-component-panel-notifications>
    <application-component-panel-announcements></application-component-panel-announcements>
<% end %>


<%# Loading standar help panels %>
<%# Temporary disable while moving to vue3 %>
<% if defined?(CloudHelp) %>
    <application-component-panel-tickets :instance-endpoint="'<%= lesli_instance_code == "deutsche_leibrenten" ? "crm" : "help" %>'"></application-component-panel-tickets>
<% end %>


<%# Loading cloud object files panel %>
<%# Temporary disable while moving to vue3 %>
<% if defined?(CloudStorage) && defined?(DeutscheLeibrenten) && false %>
    <application-component-panel-files></application-component-panel-files>
<% end %>
