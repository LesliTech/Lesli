<footer class="application-footer">
    <nav class="navbar" aria-label="main navigation" role="navigation">
        <div class="navbar-brand">
            <a class="navbar-item">
                <%= Rails.application.config.lesli_settings["account"]["company"]["name"] %>
                &bull;
                <%= Rails.application.config.lesli_settings["account"]["company"]["tag_line"] %>
            </a>
        </div>
        <div class="navbar-menu">
            <div class="navbar-start"></div>
            <div class="navbar-end">
                <% if defined?(DeutscheLeibrenten) %>
                <span class="navbar-item has-text-grey">
                    version <%= @account[:revision][:version] %> - 
                    build <%= @account[:revision][:build] %>
                </span>
                <% end %>
                <% if defined?(LesliCloud) %>
                <a class="navbar-item" href="#">
                    Privacy
                </a>
                <% end %>
            </div>
        </div>
    </nav>
</footer>
