<p style="color: green"><%= notice %></p>

<%= render @devise_session %>

<div>
  <%= link_to "Edit this session", edit_devise_session_path(@devise_session) %> |
  <%= link_to "Back to sessions", devise_sessions_path %>

  <%= button_to "Destroy this session", @devise_session, method: :delete %>
</div>
