=begin

[[license]]
=end


require "lesli_request_helper"


RSpec.describe "GET:[[url]].json", type: :request, :unless => defined?(DeutscheLeibrenten) do

RSpec.describe "Tests for Lesli3", type: :request, :unless => defined?(DeutscheLeibrenten) do
    describe "GET:/administration.json", type: :request do
        include_context "request user authentication"

        # test cases

        it "is expected to respond with successful" do
            get("[[url]].json")

            # shared examples
            expect_response_with_successful

        end

        it "is expected to validate response" do
            get("[[url]].json")

            # shared examples
            expect_response_with_successful

            puts "respuesta index #{response_body}"
        end
    end
end
