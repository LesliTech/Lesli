development:
    pool: 30
    host: 127.0.0.1
    adapter: postgresql
    encoding: unicode
    reconnect: false
    database: leslicloud_development
    username: lesli_cloud
    password: lesli_cloud
    migrations_paths:
        - "db/migrate"
        - "engines/CloudPanel/db/migrate"
        - "engines/CloudTeam/db/migrate"

test:
    pool: 30
    host: 127.0.0.1
    adapter: postgresql
    encoding: unicode
    reconnect: false
    database: leslicloud_test
    username: lesli_cloud
    password: lesli_cloud
    migrations_paths:
        - "db/migrate"
        - "engines/CloudPanel/db/migrate"
        - "engines/CloudTeam/db/migrate"

production:
    pool: 30
    host: 127.0.0.1
    adapter: postgresql
    encoding: unicode
    reconnect: false
    database: <%= Rails.application.credentials.production[:database][:database] %>
    username: <%= Rails.application.credentials.production[:database][:username] %>
    password: <%= Rails.application.credentials.production[:database][:password] %>
    migrations_paths:
        - "db/migrate"
        - "engines/CloudPanel/db/migrate"
        - "engines/CloudTeam/db/migrate"
