require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/system_descriptor/privileges", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # SystemDescriptor::Privilege. As you add validations to SystemDescriptor::Privilege, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      SystemDescriptor::Privilege.create! valid_attributes
      get system_descriptor_privileges_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      privilege = SystemDescriptor::Privilege.create! valid_attributes
      get system_descriptor_privilege_url(privilege)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_system_descriptor_privilege_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      privilege = SystemDescriptor::Privilege.create! valid_attributes
      get edit_system_descriptor_privilege_url(privilege)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new SystemDescriptor::Privilege" do
        expect {
          post system_descriptor_privileges_url, params: { system_descriptor_privilege: valid_attributes }
        }.to change(SystemDescriptor::Privilege, :count).by(1)
      end

      it "redirects to the created system_descriptor_privilege" do
        post system_descriptor_privileges_url, params: { system_descriptor_privilege: valid_attributes }
        expect(response).to redirect_to(system_descriptor_privilege_url(SystemDescriptor::Privilege.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new SystemDescriptor::Privilege" do
        expect {
          post system_descriptor_privileges_url, params: { system_descriptor_privilege: invalid_attributes }
        }.to change(SystemDescriptor::Privilege, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post system_descriptor_privileges_url, params: { system_descriptor_privilege: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested system_descriptor_privilege" do
        privilege = SystemDescriptor::Privilege.create! valid_attributes
        patch system_descriptor_privilege_url(privilege), params: { system_descriptor_privilege: new_attributes }
        privilege.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the system_descriptor_privilege" do
        privilege = SystemDescriptor::Privilege.create! valid_attributes
        patch system_descriptor_privilege_url(privilege), params: { system_descriptor_privilege: new_attributes }
        privilege.reload
        expect(response).to redirect_to(system_descriptor_privilege_url(privilege))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        privilege = SystemDescriptor::Privilege.create! valid_attributes
        patch system_descriptor_privilege_url(privilege), params: { system_descriptor_privilege: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested system_descriptor_privilege" do
      privilege = SystemDescriptor::Privilege.create! valid_attributes
      expect {
        delete system_descriptor_privilege_url(privilege)
      }.to change(SystemDescriptor::Privilege, :count).by(-1)
    end

    it "redirects to the system_descriptor_privileges list" do
      privilege = SystemDescriptor::Privilege.create! valid_attributes
      delete system_descriptor_privilege_url(privilege)
      expect(response).to redirect_to(system_descriptor_privileges_url)
    end
  end
end
