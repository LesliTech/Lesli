# CLI Tools

```
    reset passwords
    block/unblock users
    inactivate all users
    request password reset for all users
    change password for all users
    approve invitation for pending users
    activate maintenance mode
    version of all engines
    disable cron tasks
    enable cron tasks
    run all cron tasks
```
